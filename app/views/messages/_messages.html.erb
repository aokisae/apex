<ul class="list-unstyled">
  <% messages.each do |message| %>
  <div>
    <%= message.user.name%><span class="text-muted"><%= message.created_at %></span>
  </div>
      <div>
        <p><%= message.content %></p>
      </div>
  <% end %>
  <%= paginate messages %>
</ul>